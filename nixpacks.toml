# Nixpacks Configuration for Railway
# This tells Railway how to build and run the Next.js app

[phases.setup]
nixPkgs = ['...', 'bun']

[phases.install]
cmd = 'bun install'

[phases.build]
cmd = 'bun run build'

[start]
cmd = 'bun run start'
